# Copy to supabase/.env.stripe (gitignored), paste your LIVE keys from Stripe Dashboard,
# then run: npm run secrets:set:stripe
#
# Stripe Dashboard → toggle OFF "Test mode" → Developers → API keys
# Webhooks → Add endpoint (live):
#   https://gogpjxxsrcjpbugocvnd.supabase.co/functions/v1/stripe-webhook
# Events: account.updated, payment_intent.succeeded

STRIPE_SECRET_KEY=sk_live_YOUR_SECRET_KEY
STRIPE_WEBHOOK_SECRET=whsec_YOUR_LIVE_WEBHOOK_SIGNING_SECRET
STYLD_PLATFORM_FEE_PERCENT=1
