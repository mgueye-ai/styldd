/**
 * Reference copy of the Supabase shape. Prefer editing the tracked file
 * `js/supabase-config.local.js` (uncomment `url` / `anonKey` there).
 *
 * Dashboard: Project Settings → API. Project display name “Your Salon Name” is optional
 * under General (it does not change the API URL).
 *
 * Full checklist: `supabase/SETUP.md`
 */
window.__SALON_SITE_SUPABASE = {
  url: "https://YOUR_PROJECT_REF.supabase.co",
  anonKey: "YOUR_ANON_PUBLIC_KEY",
};
