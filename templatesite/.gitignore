# Supabase anon key (never commit — copy from js/supabase-config.example.js)
js/supabase-config.local.js
js/styld-tenant-config.local.js

# Stripe / local overrides (secrets stay local)
js/stripe-config.local.js

# Local env (never commit)
.env
.env.*
!.env.example
.vercel
