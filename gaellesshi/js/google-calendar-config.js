/**
 * Google Calendar used for Hair by Nadjae (appointments calendar).
 * The ID is not a secret; API access still requires OAuth/backend. When you add a
 * Supabase Edge Function for FreeBusy, set availabilityUrl to that function's URL.
 */
window.__NADJAE_GOOGLE_CALENDAR = {
  /** Paste your Google Calendar ID here when wiring availability (optional). */
  calendarId: "",
  /** Optional: e.g. "https://xxx.supabase.co/functions/v1/calendar-availability" */
  availabilityUrl: "",
};
